[project]
name = "momentum-worker"
version = "0.1.0"
description = "Celery worker for Momentum."
requires-python = ">=3.11"
dependencies = [
  "celery>=5.4"
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
